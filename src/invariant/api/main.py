"""FastAPI application entrypoint."""

# TODO: create the FastAPI app instance (`app = FastAPI()`) and mount the
# resource routers (sources, documents, controls, diff) once the storage
# layer exists.
