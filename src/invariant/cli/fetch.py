# TODO: `invariant fetch <source>` -- download a source document and preserve the raw artifact.

from invariant import source


def fetch(source_name: str):
    # download the source document and preserve the raw artifact
    # save on data/raw/<source_name>_<timestamp>.<ext>
    # calc hash sha-256
    # save source, timestamp, path and hash on database
    

